/// ═══════════════════════════════════════════════════════════════════════════
/// 📌 AppConfig - إعدادات التطبيق المركزية (Configuration Layer)
/// ═══════════════════════════════════════════════════════════════════════════
///
/// 🎯 الهدف من هذا الملف:
/// هذا الملف يحتوي على جميع الإعدادات الثابتة (Constants) للتطبيق
/// 
/// 📍 الموقع في Architecture:
/// Configuration Layer (خارج الطبقات الثلاث الرئيسية)
/// يُستخدم من جميع الطبقات
///
/// 💡 لماذا نستخدم ملف إعدادات مركزي؟
/// ✅ سهولة التعديل: تغيير رقم واحد بدلاً من البحث في 50 ملف
/// ✅ الوضوح: كل الإعدادات في مكان واحد
/// ✅ تجنب الأخطاء: لا تكرار للقيم (DRY Principle)
/// ✅ البيئات المختلفة: يمكن إنشاء AppConfig.dev و AppConfig.prod
///
/// 🔧 كيف نستخدمه؟
/// في أي ملف: AppConfig.maxDeviationDistance
/// مثال: if (distance > AppConfig.maxDeviationDistance) { alert() }
///
/// ⚠️ ملاحظة مهمة:
/// private constructor: AppConfig._()
/// معناه: لا يمكن إنشاء instance من هذا الكلاس
/// السبب: نريد فقط استخدام static constants (لا نحتاج objects)
///
class AppConfig {
  /// 🔒 Private Constructor
  /// لماذا؟ لأننا لا نريد إنشاء objects من AppConfig
  /// نريد فقط استخدام الـ static constants
  /// 
  /// مثال خاطئ:
  /// final config = AppConfig(); // ❌ Error!
  /// 
  /// مثال صحيح:
  /// final name = AppConfig.appName; // ✅
  AppConfig._();

  /// ═══════════════════════════════════════════════════════════════════════
  /// 📱 معلومات التطبيق الأساسية
  /// ═══════════════════════════════════════════════════════════════════════

  /// 🏷️ اسم التطبيق بالإنجليزية
  /// يُستخدم في:
  /// - عنوان MaterialApp
  /// - الإشعارات
  /// - رسائل البريد الإلكتروني
  static const String appName = 'Personal Security Guard';

  /// 🏷️ اسم التطبيق بالعربية
  /// يُستخدم في:
  /// - الواجهة العربية
  /// - الإشعارات المحلية
  static const String appNameAr = 'حارسي الشخصي';

  /// 📦 إصدار التطبيق الحالي
  /// يُستخدم في:
  /// - عن التطبيق (About Page)
  /// - التحقق من التحديثات
  /// - سجلات الأخطاء (Crash Reports)
  /// 
  /// الصيغة: MAJOR.MINOR.PATCH
  /// 1.0.0 = الإصدار الأول
  /// 1.1.0 = ميزات جديدة
  /// 1.0.1 = إصلاحات أخطاء
  static const String appVersion = '1.0.0';

  /// ═══════════════════════════════════════════════════════════════════════
  /// 🌐 إعدادات API (للخدمات الخارجية)
  /// ═══════════════════════════════════════════════════════════════════════

  /// 🔗 رابط API الأساسي
  /// يُستخدم عند استدعاء الخوادم الخارجية
  /// مثال: "$apiBaseUrl/$apiVersion/users"
  /// النتيجة: "https://api.psga.app/v1/users"
  /// 
  /// ملاحظة: حالياً التطبيق يستخدم Firebase فقط
  /// لكن يمكن إضافة API مخصص لاحقاً
  static const String apiBaseUrl = 'https://api.psga.app';

  /// 📌 إصدار API
  /// يُستخدم للتوافق مع إصدارات مختلفة من API
  /// v1 = الإصدار الأول
  /// v2 = إصدار محدث (لاحقاً)
  static const String apiVersion = 'v1';

  /// ═══════════════════════════════════════════════════════════════════════
  /// 🚨 إعدادات الأمان والتنبيهات
  /// ═══════════════════════════════════════════════════════════════════════

  /// 📏 الحد الأقصى للانحراف عن المسار (بالأمتار)
  /// 
  /// السيناريو:
  /// المستخدم يسير على مسار محدد
  /// إذا ابتعد 100 متر عن المسار → تنبيه!
  /// 
  /// يُستخدم في:
  /// - DeviationDetector
  /// - Active Trip Page
  /// - Alert System
  /// 
  /// مثال:
  /// المسار: من البيت للعمل
  /// المستخدم انحرف: 50 متر → لا تنبيه ✅
  /// المستخدم انحرف: 150 متر → تنبيه! ⚠️
  static const int maxDeviationDistance = 100;

  /// ⏱️ وقت العد التنازلي للتنبيه (بالثواني)
  /// 
  /// السيناريو:
  /// التطبيق اكتشف انحراف عن المسار
  /// يبدأ عد تنازلي: 30، 29، 28...
  /// إذا لم يلغي المستخدم → إرسال تنبيه تلقائي
  /// 
  /// يُستخدم في:
  /// - Alert Countdown Widget
  /// - SOS System
  /// 
  /// مثال:
  /// 30 ثانية = نصف دقيقة للمستخدم لإلغاء التنبيه
  static const int alertCountdownSeconds = 30;

  /// 🔄 الفترة الزمنية للمزامنة التلقائية (بالدقائق)
  /// 
  /// السيناريو:
  /// التطبيق يزامن البيانات بين Hive (محلي) و Firebase (سحابي)
  /// كل 5 دقائق → تحقق من التحديثات
  /// 
  /// يُستخدم في:
  /// - SyncManager
  /// - Background Services
  /// 
  /// لماذا 5 دقائق؟
  /// - ليس سريعاً جداً (يستهلك بطارية)
  /// - ليس بطيئاً جداً (البيانات محدثة نسبياً)
  static const int syncIntervalMinutes = 5;

  /// ═══════════════════════════════════════════════════════════════════════
  /// 📍 إعدادات الموقع الجغرافي (GPS)
  /// ═══════════════════════════════════════════════════════════════════════

  /// 🕐 فترة تحديث الموقع (بالثواني)
  /// 
  /// السيناريو:
  /// أثناء الرحلة النشطة، التطبيق يستقبل الموقع من GPS
  /// كل 10 ثواني → تحديث الموقع على الخريطة
  /// 
  /// يُستخدم في:
  /// - LocationService
  /// - Active Trip Tracking
  /// - Deviation Detection
  /// 
  /// لماذا 10 ثواني؟
  /// - دقة جيدة (نعرف موقع المستخدم بسرعة)
  /// - لا يستهلك بطارية كثيراً (ليس كل ثانية!)
  static const int locationUpdateIntervalSeconds = 10;

  /// 🎯 الحد الأدنى لدقة الموقع (بالأمتار)
  /// 
  /// السيناريو:
  /// GPS قد يعطي موقع غير دقيق (بسبب الطقس، المباني، إلخ)
  /// إذا كانت الدقة أكثر من 20 متر → تجاهل!
  /// 
  /// يُستخدم في:
  /// - LocationService.getCurrentPosition()
  /// - تصفية البيانات غير الدقيقة
  /// 
  /// مثال:
  /// GPS: موقعك دقيق ±10 متر → مقبول ✅
  /// GPS: موقعك دقيق ±50 متر → مرفوض ❌ (غير موثوق)
  static const int minAccuracyMeters = 20;

  /// ═══════════════════════════════════════════════════════════════════════
  /// 🔢 حدود البيانات (Limits)
  /// ═══════════════════════════════════════════════════════════════════════

  /// 📱 الحد الأقصى لعدد جهات الاتصال الطارئة
  /// 
  /// السبب:
  /// - لا نريد إرسال تنبيهات لـ 50 شخص!
  /// - 5 أشخاص كافي (عائلة + أصدقاء مقربين)
  /// 
  /// يُستخدم في:
  /// - Add Contact Page
  /// - Validation في ContactBloc
  /// 
  /// مثال:
  /// المستخدم لديه 5 جهات اتصال بالفعل
  /// يحاول إضافة السادس → رسالة خطأ: "الحد الأقصى 5"
  static const int maxContactsCount = 5;

  /// 🗺️ الحد الأقصى لعدد المسارات المحفوظة
  /// 
  /// السبب:
  /// - توفير مساحة التخزين
  /// - تجنب التشويش (10 مسارات كافية)
  /// 
  /// يُستخدم في:
  /// - Create Route Page
  /// - Validation في RouteBloc
  /// 
  /// مثال:
  /// المستخدم لديه 10 مسارات بالفعل
  /// يحاول إنشاء الحادي عشر → رسالة: "احذف مساراً قديماً أولاً"
  static const int maxRoutesCount = 10;

  /// ═══════════════════════════════════════════════════════════════════════
  /// 🛠️ إعدادات التطوير (Development Settings)
  /// ═══════════════════════════════════════════════════════════════════════

  /// 🐛 تفعيل وضع التصحيح (Debug Mode)
  /// 
  /// true = طباعة جميع السجلات (Logs) في Console
  /// false = لا تطبع سجلات (في Production)
  /// 
  /// يُستخدم في:
  /// - AppLogger
  /// - جميع ملفات التطبيق
  /// 
  /// ملاحظة مهمة:
  /// قبل نشر التطبيق → غيّر إلى false!
  static const bool enableDebugMode = true;

  /// 📊 تفعيل Firebase Crashlytics
  /// 
  /// true = إرسال تقارير الأخطاء لـ Firebase
  /// false = لا ترسل تقارير (في Development)
  /// 
  /// يُستخدم في:
  /// - Error Handlers
  /// - main.dart
  /// 
  /// ملاحظة:
  /// في Development: false (لا نريد تقارير أخطاء التطوير)
  /// في Production: true (نريد معرفة الأخطاء الحقيقية)
  static const bool enableCrashlytics = false;

  /// ═══════════════════════════════════════════════════════════════════════
  /// 🔥 إعدادات Firebase
  /// ═══════════════════════════════════════════════════════════════════════

  /// 🔥 تفعيل Firebase
  /// 
  /// true = استخدام Firebase للبيانات البعيدة
  /// false = العمل بدون Firebase (Local Only)
  /// 
  /// يُستخدم في:
  /// - main.dart (قرار تهيئة Firebase)
  /// - Dependency Injection
  /// 
  /// متى نستخدم true؟
  /// - في Production (التطبيق الحقيقي)
  /// - عند اختبار المزامنة
  /// 
  /// متى نستخدم false؟
  /// - عند العمل بدون إنترنت
  /// - عند اختبار Offline Mode
  static const bool enableFirebase = true;

  /// 🧪 استخدام بيانات Mock للمصادقة
  /// 
  /// true = استخدام Mock Auth (بيانات وهمية)
  /// false = استخدام Firebase Auth (حقيقي)
  /// 
  /// يُستخدم في:
  /// - main.dart
  /// - Dependency Injection (اختيار DataSource)
  /// 
  /// الفائدة:
  /// في Development: true
  /// - لا نحتاج Firebase
  /// - تسجيل دخول فوري (بدون انتظار)
  /// - لا نستهلك Firebase Quota
  /// 
  /// في Production: false
  /// - استخدام Firebase Auth الحقيقي
  static const bool useMockAuth = false;

  /// 💾 استخدام بيانات Mock للتخزين
  /// 
  /// true = استخدام Mock Storage (بيانات في الذاكرة فقط)
  /// false = استخدام Hive (تخزين حقيقي على الجهاز)
  /// 
  /// يُستخدم في:
  /// - Dependency Injection (اختيار Local DataSource)
  /// 
  /// الفائدة:
  /// true: للاختبار السريع (بدون تأثير على Hive)
  /// false: للتطوير العادي (بيانات حقيقية)
  static const bool useMockStorage = false;

  /// ═══════════════════════════════════════════════════════════════════════
  /// 🔄 إعدادات المزامنة المتقدمة
  /// ═══════════════════════════════════════════════════════════════════════

  /// 📦 حجم دفعة المزامنة (عدد العناصر)
  /// 
  /// السيناريو:
  /// لدينا 100 مسار لمزامنتهم مع Firebase
  /// بدل إرسالهم دفعة واحدة (ثقيل!)
  /// نرسل 10 مسارات في كل مرة
  /// 
  /// يُستخدم في:
  /// - SyncService.syncBatch()
  /// 
  /// لماذا 10؟
  /// - لا يثقل الشبكة
  /// - لا يأخذ وقت طويل
  /// - يمكن إلغاء المزامنة في منتصف الطريق
  static const int syncBatchSize = 10;

  /// 🔁 الحد الأقصى لإعادة محاولات المزامنة
  /// 
  /// السيناريو:
  /// المزامنة فشلت (مثلاً: انقطع الإنترنت)
  /// نحاول مرة أخرى... ومرة أخرى... حتى 3 مرات
  /// بعد 3 محاولات → نتوقف (حتى لا نهدر موارد)
  /// 
  /// يُستخدم في:
  /// - SyncService.syncWithRetry()
  /// 
  /// مثال:
  /// محاولة 1: فشل (لا إنترنت)
  /// محاولة 2: فشل (لا إنترنت)
  /// محاولة 3: نجح! ✅
  static const int syncMaxRetries = 3;

  /// ⏱️ وقت انتهاء المزامنة (بالثواني)
  /// 
  /// السيناريو:
  /// بدأنا مزامنة بيانات مع Firebase
  /// إذا مر 30 ثانية بدون نتيجة → إلغاء!
  /// 
  /// يُستخدم في:
  /// - HTTP Requests
  /// - Firebase Operations
  /// 
  /// لماذا 30 ثانية؟
  /// - كافي لأغلب العمليات
  /// - ليس طويلاً جداً (المستخدم ينتظر)
  static const int syncTimeoutSeconds = 30;

  /// ═══════════════════════════════════════════════════════════════════════
  /// ⚙️ الإعدادات الافتراضية للمستخدم
  /// ═══════════════════════════════════════════════════════════════════════

  /// 📋 الإعدادات الافتراضية عند إنشاء حساب جديد
  /// 
  /// يُستخدم عند:
  /// - تسجيل مستخدم جديد
  /// - إعادة تعيين الإعدادات
  /// 
  /// الإعدادات:
  /// - language: 'ar' = اللغة العربية افتراضياً
  /// - theme: 'light' = المظهر الفاتح
  /// - notifications: true = الإشعارات مفعلة
  /// - sound: true = الصوت مفعل
  /// - vibration: true = الاهتزاز مفعل
  /// - autoStartTrip: false = لا تبدأ الرحلة تلقائياً (يدوي)
  /// - shareLocation: true = مشاركة الموقع مع جهات الاتصال
  /// 
  /// مثال استخدام:
  /// ```dart
  /// final settings = AppConfig.defaultSettings;
  /// final language = settings['language'] as String; // 'ar'
  /// ```
  static const Map<String, dynamic> defaultSettings = {
    'language': 'ar',           // 🌍 اللغة
    'theme': 'light',           // 🎨 المظهر
    'notifications': true,      // 🔔 الإشعارات
    'sound': true,              // 🔊 الصوت
    'vibration': true,          // 📳 الاهتزاز
    'autoStartTrip': false,     // 🚗 بدء الرحلة تلقائياً
    'shareLocation': true,      // 📍 مشاركة الموقع
  };
}